/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CashRegisterActionResponse } from '../../models/cash-register-action-response';
import { CloseCashRegisterRequest } from '../../models/close-cash-register-request';

export interface CloseCashRegister$Params {
      body: CloseCashRegisterRequest
}

export function closeCashRegister(http: HttpClient, rootUrl: string, params: CloseCashRegister$Params, context?: HttpContext): Observable<StrictHttpResponse<CashRegisterActionResponse>> {
  const rb = new RequestBuilder(rootUrl, closeCashRegister.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CashRegisterActionResponse>;
    })
  );
}

closeCashRegister.PATH = '/api/v1/cash-register/close';
